import { css } from "linaria";
import * as React from "react";

export const AmilyPage: React.FC = () => (
  <h1 className={pageStyle}>Page Amily from Application A</h1>
);

const pageStyle = css`
  color: #111184;
`;
